Conversion of a raw file into tif file
======================================

Individual tif files should be prefered when saving the conversion result
because the RGB only supports uint8 but the raw images are uint16.

.. literalinclude:: ../../examples/convert_raw.py
